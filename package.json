{
  "name": "ux-review",
  "version": "0.3.3",
  "description": "Claude Code plugin for AI-driven UX reviews: scripted browser sessions, narrated video, findings report",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && npm run bundle",
    "typecheck": "tsc --noEmit",
    "compose": "node --loader ts-node/esm lib/compose-session.ts",
    "mcp-server": "node dist/mcp-server/bundle.js",
    "bundle": "esbuild dist/mcp-server/index.js --bundle --platform=node --target=node20 --format=esm --outfile=dist/mcp-server/bundle.js --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1"
  },
  "license": "GPL-2.0"
}
