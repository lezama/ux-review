{
  "name": "ux-simulator",
  "version": "0.3.0",
  "description": "Standalone UX simulation and recording service for web apps",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "compose": "node --loader ts-node/esm lib/compose-session.ts",
    "mcp-server": "node dist/mcp-server/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1"
  }
}
